BUG 

values : renvoie d'office un array : donc si premier objet = aray = alors retour = array d'array.

run : doit retourner retour seul (pas array) si un seul call avant

____________________________________________


CORE :

PILOU : 

	add "direct" flag in store options that return direct repsonse from store in place of a deep chain
	==> refactor stores to permit that
____________________________________________________________________________________________________


deep.stores : test all
	add jsonp capabilities (jsonp::...)

	readOnly, serveSchema, manage privates + sanitisation as Option (need sanitizer lib)

	rpc, bulk for local stores

	jstorage / cookies


	add metas informations in nodes

	SYNCHED store !!!
		save locally bunch of object : synch it with remote sto


deep.context
	add chain handlers to modify context


Errors 
	deep should return HTTP styled formatted errors (status, body)


Deep Linking :  ROUTES
	work on pattern : finalise it
	implement routes in deep-routes


data binders : 
	work on EditableHtml with schema

	modularise binders


deep sheet
	


//_________________________


implémentation jsgi like : 


ajout chain handler : 
	
	forme d'around : 

		.around(function(chain){
			return function(arg){
				if(arg...)
				{
					//... process the arg (as request in jsgi)
					return reponse;
				}
				return base.call(this, arg);
			}
		})

		deep(request)
		.done(function(req){
			return req;
		})

